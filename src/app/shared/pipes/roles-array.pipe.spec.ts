import { RolesArrayPipe } from './roles-array.pipe';

describe('RolesArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new RolesArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
